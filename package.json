{
  "name": "tributary",
  "version": "0.1.0",
  "private": true,
  "description": "Git-native Markdown notebook workspace. Local-first React Electron application over one Git repository per workspace.",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build && node scripts/check-electron-boundary.mjs",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "dev": "pnpm run -r --parallel --filter='./{packages,apps}/*' dev",
    "clean": "find . -name node_modules -type d -prune -exec rm -rf {} + && find . -name 'dist' -type d -prune -exec rm -rf {} +"
  }
}